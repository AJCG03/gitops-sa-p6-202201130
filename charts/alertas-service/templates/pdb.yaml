apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: alertas-service-pdb
  namespace: {{ .Values.global.namespace }}
  labels:
    app: alertas-service
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: alertas-service
